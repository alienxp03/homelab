---
# Secret created separately with kubectl
# kubectl create secret generic bazarr-api-key -n media --from-literal=api-key='YOUR_API_KEY'
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: exportarr-bazarr
  namespace: media
  labels:
    app: exportarr-bazarr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: exportarr-bazarr
  template:
    metadata:
      labels:
        app: exportarr-bazarr
    spec:
      containers:
        - name: exportarr
          image: ghcr.io/onedr0p/exportarr:v2.0.1
          args:
            - bazarr
            - --port
            - "9710"
            - --url
            - "http://bazarr.media:6767"
            - --api-key-file
            - "/secrets/api-key"
          ports:
            - containerPort: 9710
              name: metrics
          volumeMounts:
            - name: api-key
              mountPath: /secrets
              readOnly: true
          resources:
            requests:
              memory: 64Mi
              cpu: 50m
            limits:
              memory: 128Mi
              cpu: 100m
          livenessProbe:
            httpGet:
              path: /healthz
              port: metrics
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: metrics
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: api-key
          secret:
            secretName: bazarr-api-key
---
apiVersion: v1
kind: Service
metadata:
  name: exportarr-bazarr
  namespace: media
  labels:
    app: exportarr-bazarr
spec:
  selector:
    app: exportarr-bazarr
  ports:
    - name: metrics
      port: 9710
      targetPort: 9710
