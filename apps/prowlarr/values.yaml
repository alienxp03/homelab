controllers:
  main:
    type: deployment
    replicas: 1
    containers:
      main:
        image:
          repository: lscr.io/linuxserver/prowlarr
          tag: latest
        env:
          TZ: Asia/Kuala_Lumpur
          PUID: "1028"
          PGID: "100"

service:
  main:
    ports:
      http:
        port: 9696
        targetPort: 9696

ingress:
  main:
    enabled: false
    className: traefik
    hosts:
      - host: prowlarr.homelab.azuanz.com
        paths:
          - path: /
            pathType: Prefix
    tls:
      - secretName: homelab-certificate-secret
        hosts:
          - prowlarr.homelab.azuanz.com

persistence:
  config:
    enabled: true
    type: persistentVolumeClaim
    size: 2Gi
    storageClass: local-path
    accessMode: ReadWriteOnce
    globalMounts:
      - path: /config

resources:
  requests:
    cpu: 200m
    memory: 256Mi
  limits:
    cpu: "2"
    memory: 2Gi
